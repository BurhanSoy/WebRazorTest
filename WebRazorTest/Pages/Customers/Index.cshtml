@page
@model WebRazorTest.Pages.Customers.IndexModel
@{
}


<h1>Contacts home page</h1>
<div>
    <a asp-page="Create">CREATE Customer</a>
</div>
<div>
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        @if (Model.Customers != null)
        {
            foreach (var cust in Model.Customers)
            {
                <tr>
                    <td>@cust.Id </td>
                    <td>@cust.Name</td>
                    <td>
                        <a asp-page="./Edit" asp-route-id="@cust.Id">Edit</a> |
                        <a asp-page="./Details" asp-route-id="@cust.Id">Details</a> |
                        <a asp-page="./Delete" asp-route-id="@cust.Id">Delete</a> |
                    </td>
                </tr>
            }
        }
        </tbody>
    </table>
</div>